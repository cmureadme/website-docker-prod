services:
    django:
        build: .
        expose:
            - "8000"
        volumes:
            - ./.env:/readme-website/.env
            - ./media:/readme-website/media
            - ./db.sqlite3:/readme-website/db.sqlite3
        restart: unless-stopped

    nginx:
        image: nginx:latest
        ports:
            - "8000:80"
        volumes:
            - ./nginx.conf:/etc/nginx/nginx.conf:ro
            - ./readme-website/static:/static:ro
            - ./media:/media:ro
            - ./sample_dbs:/sample_dbs:ro
        depends_on:
            - django
